import styled from 'styled-components';

const StyledTitle = styled.p`
  color: #333;
  overflow: hidden;
  height: 42px;
  margin-bottom: 8px;
  font-size: 18px;
  line-height: 22px;
  font-weight: 500;
`;

export default StyledTitle;
